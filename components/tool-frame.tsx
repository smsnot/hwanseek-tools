import type { ReactNode } from 'react';
const tools = [['/', '001 · PDF line breaks'], ['/list-count-review', '002 · List counts'], ['/screenshot-cut-review', '003 · Screenshot cuts'], ['/subtitle-pair-review', '004 · Subtitle names'], ['/pdf-highlight-review', '005 · PDF highlights'], ['/image-fit-review','006 · Image fit'], ['/image-size-check','007 · Image size'], ['/photo-sheet','008 · Photo sheet']];
export function ToolNav({ current }: { current: string }) { return <nav className="tool-nav" aria-label="Tools">{tools.map(([href, title]) => <a key={href} href={href} aria-current={href === current ? 'page' : undefined}>{title}</a>)}</nav>; }
export function ToolFrame({ current, number, title, intro, children }: { current: string; number: string; title: string; intro: string; children: ReactNode }) {
  return <div className="site-shell file-tool"><header className="site-header"><a className="wordmark" href="/">HWAN<span>SEEK</span><span className="brand-dot" aria-hidden="true" /></a><nav aria-label="Main"><a href="https://hwanseek.blogspot.com/">Field notes ↗</a><a href="https://github.com/smsnot/hwanseek-tools">Source ↗</a></nav></header><main id="main"><ToolNav current={current} /><div className="title-row"><div><p className="eyebrow">TOOL {number}</p><h1>{title}</h1><p className="intro">{intro}</p></div><span className="local-badge">Free · runs in your browser</span></div>{children}</main><footer><span>HWANSEEK · Small tools for everyday problems</span><a href="https://www.instagram.com/hwan_seek/">Follow the experiments ↗</a></footer></div>;
}
